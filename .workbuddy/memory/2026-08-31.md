# 2026-08-31

- 代码走读：梳理混合检索（BM25 + Dense + RRF）在项目中的位置与触发时机。
  - 实现：MemoryCore `src/core/tools/memory-search.ts`（L1）、`conversation-search.ts`（L0）、`hooks/auto-recall.ts`（客户端 RRF）；`store/tcvdb.ts`、`store/tcvdb-skill-store.ts`（TCVDB 原生 hybridSearch + 服务端 RRF k=60）；通用 `store/search-utils.ts` rrfMerge。
  - 触发路径：① MemoryProxy `injection/injectors/tdai-tools-injector.ts` 静态注入工具说明，LLM 按需 curl memory-bridge（`/v3/atomic/search`、`/v3/conversation/search`）——现网主路径，L0/L1 已不再每轮自动召回（保护 prompt cache，见 injection/index.ts:349 L1 recall injector 已下线）；② MemoryCore gateway `POST /recall`（v1，每轮 LLM 前 before_prompt_build / Hermes prefetch 触发 performAutoRecall，userText 非空 + cfg.recall.strategy 默认 hybrid）；③ SDK / 面板直调 v2/v3 搜索端点。
- 代码走读：确认分层存储。L0/L1 走检索型存储（store/factory.ts：`tcvdb` 后端 = 腾讯向量数据库，dense + BM25 稀疏向量；`sqlite` 后端 = SQLite + sqlite-vec + FTS5）；L2/L3 走注入型文件存储（`core/storage/types.ts` StoragePaths：`scene_blocks/` 场景块 + `persona.md`，经 StorageAdapter 存本地 FS/COS，按 path 直接读，不走相似度检索；v2-router `syncProfileToVdb` 仅把 L2/L3 版本快照同步进 VDB profiles collection 供版本管理/审计）。
- 代码走读：本地部署 embedding 配置结论（config.ts:410-475 + factory.ts:99-126 + embedding.ts）。默认 `provider: "none"` → 无向量、仅 FTS5 BM25（vec0 不建）；`provider: "local"` 已在用户配置层禁用（报错降级 none，embedding.ts 的 LocalEmbeddingService / embeddinggemma-300m Q8_0 GGUF 768 维代码保留但不可达，factory 仅在 enabled && provider!="local" && apiKey 时创建服务）；启用向量需配 OpenAI 兼容远程 API（openai/deepseek/qclaw 等，apiKey+baseUrl+model+dimensions，示例 text-embedding-3-small/1536）。TCVDB 模式走服务端 embedding（默认 bge-large-zh），客户端 NoopEmbeddingService。
- 代码走读：docker 部署数据落盘位置（deploy/global-images/start-memory-core.sh + MemoryCore/Dockerfile）。named volume `tdai-memory-core-data` → /data/tdai-memory（TDAI_DATA_DIR / data.baseDir），包含：vectors.db（L0/L1 向量+FTS5，default 实例 dataDir/vectors.db，其他实例 dataDir/instances/{id}/vectors.db，store-pool.ts:405-412）、tdai_metadata_*/metadata.db（db-name.ts:53）、scene_blocks/ + persona.md（L2/L3）、skills/。不在卷内：gateway config（宿主 .memory-core-config/ 只读挂载）、.admin-key（宿主机，卷清理需同步删）；memory-hub 用独立卷 tdai-panel-data；proxy.db（/data/tdai-memory-proxy/proxy.db）在 start-proxy.sh 中未挂卷，容器重建即丢（注意点）。
- 代码走读：数据落进 volume 的完整代码链路。① start-memory-core.sh L55-122 生成 yaml（data.baseDir: /data/tdai-memory）、L129 `-v ${MEMORY_CORE_VOLUME}:/data/tdai-memory`（named volume 默认 tdai-memory-core-data，.env 可改 MEMORY_CORE_VOLUME）、L134 `-e TDAI_DATA_DIR=/data/tdai-memory`；config.yaml 走 bind mount ro（L130，不在卷内）。② Dockerfile L139 `mkdir -p /data/tdai-memory /data/config`、L147 ENV TDAI_DATA_DIR 默认值。③ 应用侧唯一出口 `gateway/config.ts:434`：`rawBaseDir = env("TDAI_DATA_DIR") ?? yaml.data.baseDir ?? resolveDefaultDataDir()`，L436 展开 ~ → baseDir。④ 双通道分流：通道 A（L0/L1）store-pool.ts:408-413 `default → {baseDir}/vectors.db`、其他 → `{baseDir}/instances/{id}/vectors.db`，sqlite.ts:446-449 构造前 mkdirSync 父目录；通道 B（L2/L3）server.ts:624 `new LocalStorageBackend(baseDir)` + pipeline-factory.ts:219-225 initDataDirectories（mkdir conversations/records/scene_blocks/.metadata/.backup），local-backend.ts resolvePath(key)=resolve(rootDir,key)（防路径穿越），putObject=mkdir+writeFile、appendObject=appendFile。⑤ 因所有写入都以 /data/tdai-memory 为根，恰好全部落在 volume 挂载点 → 由 Docker 持久化到宿主 /var/lib/docker/volumes/tdai-memory-core-data/_data（Windows Desktop 在 WSL2 内）。
- 代码走读：provider="none" 时卷内**没有** embedding 向量，"vectors.db" 只是文件名。证据：① sqlite.ts:682/804 `if (this.dimensions > 0)` 才建 l1_vec/l0_vec（vec0），dims=0 时跳过且 vecTablesReady=false（1194 行），searchL1Vector 开头 `if (degraded || !vecTablesReady) return []`；② upsert 路径 sqlite.ts:1403-1411 `if (!skipVec)` 才写 vec，无 embedding 时 skipVec=true（日志 "Skipping vec write (no embedding)"，v3.x 起不再插入零向量，历史零向量注释见 1476-1482）；③ BM25 本地模式不走稀疏向量——bm25-local.ts 仅被 tcvdb.ts 引用（TCVDB 才把 BM25 编码成 sparse 向量入库），SQLite 用 FTS5 倒排索引（l1_fts/l0_fts 无条件创建，sqlite.ts:1088/1111）；④ vectors.db 实际承载：l1_records/l0_conversations 元数据（无 embedding 列）、l1_fts/l0_fts、skills/skill_fts（skill_vec 同样 dims>0 才建，skill-store-ddl.ts:9）、embedding_meta（key=embedding_provider_info）、各类索引——所以文件存在且持续增长与向量无关；⑤ 历史兼容：sqlite.ts:591-593 注释表明旧版本 provider="none" 曾建过 768D 占位 vec0 表，升级后新版本不再建，若见空 vec0 表结构可能是旧库遗留。
- 代码走读：确认"组合检索是条件性的"——当前默认配置（provider=none）实际只走 **FTS5 BM25 单路**，非组合。证据：① sqlite.ts:3464-3466 能力上报 `vectorSearch: this.vecTablesReady`（dims=0 时 false）、`nativeHybridSearch: false`；② memory-search.ts:225 向量路 `if (!hasEmbedding) return []` 直接短路（不调 embed、不进 RRF），L260-274 strategy 判定：两路都有→"hybrid"、仅 fts→"fts"、仅 vec→"embedding"，L278-286 仅 strategy==="hybrid" 才 rrfMergeL1，单路直接返回该路结果；③ TCVDB 原生混合短路条件 memory-search.ts:149 `nativeHybridSearch && searchL1Hybrid`，SQLite 不满足；④ auto-recall.ts:472-478 与 memory-search 不同——配置 hybrid 但 embedding 不可用时**不降级**，直接 throw RecallErrors.configMissingEmbedding（fast-fail）；conversation-search.ts（L0）同 memory-search 模式。结论：真组合（BM25+Dense+RRF）只在配置了 OpenAI 兼容 embedding API 的 SQLite 后端或 TCVDB 后端下发生。
- 【用户实测确认 2026-08-31】用户核对了**运行中容器**的 L0/L1 配置：实际已启用混合检索 = SQLite FTS5/BM25（关键词）+ **BGE-M3 1024 维**（向量，OpenAI 兼容 API 接入）+ 两路并行 RRF 融合；不保证每次两路——embedding 失败/无命中时退化（strategy 区分 hybrid/fts/embedding）。即：用户实际部署**不是**仓库默认 standalone.yaml 的 provider=none，而是自定义启用了 embedding；BGE-M3 只消费 dense 1024，其 sparse 能力闲置（本地稀疏召回 = FTS5 倒排，非 BGE-M3 sparse）。
- 代码走读：向量检索**不限于 L1**，L0 同样完整支持。SQLite 侧：l0_vec（vec0，sqlite.ts:806，同样 dims>0 才建）+ upsertL0(record, embedding)（sqlite.ts:1860，embedding 可选，无则只写元数据+FTS）+ searchL0Vector（sqlite.ts:2002）；tools 层 conversation-search.ts 与 memory-search.ts 完全同构（hybrid 默认、L212 `if (!hasEmbedding) return []` 短路、L246-253 strategy 判定、rrfMergeL0）。TCVDB 侧：searchL0HybridAsync 同样存在。L2/L3 不参与向量检索（文件存储直读注入）。
